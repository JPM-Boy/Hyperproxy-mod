toggle_organization = {
    effect = {
        if = {
            limit = {
                has_variable = organization_panel
            }
            remove_variable = organization_panel
        }
        else = {
            set_variable = organization_panel
        }
    }
}
toggle_imf = {
    effect = {
        if = {
            limit = {
                has_variable = imf_window
            }
            remove_variable = imf_window
            set_variable = organization_panel
        }
        else = {
            set_variable = imf_window
            remove_variable = organization_panel
            # LIST CONTRIBUTION
            clear_global_variable_list = imf_contribution_list_ordered
            ordered_in_global_list = {
                variable = imf_contribution_list
                order_by = var:imf_contribution
                max = all
                check_range_bounds = no
                add_to_global_variable_list = { name = imf_contribution_list_ordered target = THIS }
            }
            # LIST Debtors
            clear_global_variable_list = imf_debtors_list_ordered
            ordered_in_global_list = {
                variable = imf_contribution_list
                order_by = var:imf_debt
                max = all
                check_range_bounds = no
                add_to_global_variable_list = { name = imf_debtors_list_ordered target = THIS }
            }

        }
        
    }
}
contribute_imf = {
    scope = country
    effect = {
        if = {
            limit = {gold_reserves >= 1000000}
            add_treasury = -1000000
            change_variable = {
            name = imf_contribution
            add = 1
            }
        }
        # LIST CONTRIBUTION
        clear_global_variable_list = imf_contribution_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_contribution
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_contribution_list_ordered target = THIS }
        }
        # LIST Debtors
        clear_global_variable_list = imf_debtors_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_debt
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_debtors_list_ordered target = THIS }
        }
    }
    confirm_title = "your_title"    # adding this will prompt the player to confirm the action
    confirm_text = "your_text"    # additional text in the confirmation popup
    is_shown = { always = yes }
    ai_is_valid = { always = yes }   # Whether this SGUI is available to the AI or not (default false)
    ai_chance = {one}             # The chance that the AI will activate this SGUI (script value between 1 and 100)
    ai_frequency = {one}           # Named value determining how frequently the AI will evaluate this SGUI (in months)
}
contribute_100K_imf = {
    scope = country
    effect = {
        if = {
            limit = {gold_reserves >= 100000}
            add_treasury = -100000
            change_variable = {
            name = imf_contribution
            add = 0.1
            }
        }
        # LIST CONTRIBUTION
        clear_global_variable_list = imf_contribution_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_contribution
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_contribution_list_ordered target = THIS }
        }
        # LIST Debtors
        clear_global_variable_list = imf_debtors_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_debt
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_debtors_list_ordered target = THIS }
        }
    }
    confirm_title = "your_title"    # adding this will prompt the player to confirm the action
    confirm_text = "your_text"    # additional text in the confirmation popup
    is_shown = { always = yes }      # Determines whether the SGUI is shown / available to a player
    ai_is_valid = { always = yes }   # Whether this SGUI is available to the AI or not (default false)
    ai_chance = {one}             # The chance that the AI will activate this SGUI (script value between 1 and 100)
    ai_frequency = {one}           # Named value determining how frequently the AI will evaluate this SGUI (in months)
}
ask_funds_imf = {
    scope = country
    effect = {
        if = {
            limit = {infamy <= 20}
            add_treasury = 1000000
            change_variable = {
            name = imf_debt
            add = 1
            }
        }
        # LIST CONTRIBUTION
        clear_global_variable_list = imf_contribution_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_contribution
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_contribution_list_ordered target = THIS }
        }
        # LIST Debtors
        clear_global_variable_list = imf_debtors_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_debt
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_debtors_list_ordered target = THIS }
        }
    }
    confirm_title = "your_title"    # adding this will prompt the player to confirm the action
    confirm_text = "your_text"    # additional text in the confirmation popup
    is_shown = { always = yes }
    ai_is_valid = { always = yes }   # Whether this SGUI is available to the AI or not (default false)
    ai_chance = {base = 33}             # The chance that the AI will activate this SGUI (script value between 1 and 100)
    ai_frequency = 1           # Named value determining how frequently the AI will evaluate this SGUI (in months)
}

ask_funds_100K_imf = {
    scope = country
    effect = {
        if = {
            limit = {infamy <= 20}
            add_treasury = 100000
            change_variable = {
            name = imf_debt
            add = 0.1
            }
        }
        # LIST CONTRIBUTION
        clear_global_variable_list = imf_contribution_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_contribution
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_contribution_list_ordered target = THIS }
        }
        # LIST Debtors
        clear_global_variable_list = imf_debtors_list_ordered
        ordered_in_global_list = {
            variable = imf_contribution_list
            order_by = var:imf_debt
            max = all
            check_range_bounds = no
            add_to_global_variable_list = { name = imf_debtors_list_ordered target = THIS }
        }
    }
    confirm_title = "your_title"    # adding this will prompt the player to confirm the action
    confirm_text = "your_text"    # additional text in the confirmation popup
    is_shown = { always = yes }
    ai_is_valid = { always = yes }   # Whether this SGUI is available to the AI or not (default false)
    ai_chance = {base = 33}             # The chance that the AI will activate this SGUI (script value between 1 and 100)
    ai_frequency = 1           # Named value determining how frequently the AI will evaluate this SGUI (in months)
}